# 运营安装说明（Windows）

本文面向收到安装包的非技术同事。无需安装 Python。

## 你会收到的文件

- **`RealTimeTranslation-Setup.exe`**：安装程序（推荐）。由研发在发布页或网盘提供下载链接。
- （可选）**`RealTimeTranslation` 文件夹**：绿色版，需解压后进入文件夹双击 `RealTimeTranslation.exe`，一般不推荐给运营。

## 推荐步骤：下载并安装

1. 打开研发提供的 **下载链接**，保存 **`RealTimeTranslation-Setup.exe`**。
2. **双击** 安装程序。
3. 若出现 **Windows 已保护你的电脑** 或 **未知发布者**：
   - 这是未做「代码签名」时常见提示，请选择 **更多信息** → **仍要运行**（具体文案因系统版本略有不同）。
4. 按向导选择安装目录（可使用默认路径），完成安装。
5. 安装结束页可勾选 **运行 Real Time Translation**（或从开始菜单启动）。

## 首次使用

1. 打开软件后进入 **设置**，填写：
   - 转写 / 翻译的 **Base URL**、**模型名**
   - **API Key**（或使用「与转写共用」等选项，以软件内说明为准）
2. 播放有声音的直播或视频，回到主界面点击 **开始**（或 **开始翻译**），确认左侧会话与字幕是否正常。

## 数据保存在哪

- 配置与字幕记录保存在本机用户目录（由 Windows 管理），**重装系统或换电脑后需重新配置**，密钥保存在系统凭据中，一般无需手抄文件。

## 常见问题

| 现象 | 可尝试 |
|------|--------|
| 双击安装包没反应或被拦截 | 联系 IT 放行；或右键 **以管理员身份运行**（安装程序需要写入 Program Files 时可能提示管理员权限） |
| 打开后提示缺 DLL | 安装微软 **VC++ 运行库**（研发可附下载链接） |
| 杀毒软件删除 exe | 将安装目录或 exe 加入信任区 |
| 401 / Key 错误 | 在设置中重新填写 Key，确认 URL 与模型与服务商文档一致 |
| 没有声音或没有字幕 | 先确认系统正在播放声音；检查设置中的采集/设备相关项 |

## 卸载

- **设置 → 应用 → 已安装的应用** 中找到 **Real Time Translation** 卸载；或从开始菜单里的卸载快捷方式卸载。

## 需要新版本时

- 向研发索取新版 **`RealTimeTranslation-Setup.exe`**，直接安装覆盖即可（如有特别说明以研发通知为准）。
